import env from './env.js';

// Razorpay instance placeholder for Phase 6
export const razorpayConfig = {
  keyId: env.razorpay.keyId,
  keySecret: env.razorpay.keySecret
};

export default razorpayConfig;
